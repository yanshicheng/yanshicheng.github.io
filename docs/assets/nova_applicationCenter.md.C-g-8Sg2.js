import{_ as r,c as t,o as a,aM as e}from"./chunks/framework.DvFTVADX.js";const c=JSON.parse('{"title":"应用中心","description":"","frontmatter":{"title":"应用中心","author":{"name":"Yan Shicheng","link":"https://www.ikubeops.com"},"date":"2026-01-17T14:00:00.000Z","permalink":"/nova/applicationCenter","categories":["kube-nova"],"tags":["kube-nova"]},"headers":[],"relativePath":"nova/applicationCenter.md","filePath":"01.指南/10.使用/12.应用发布.md","lastUpdated":1768731971000}'),n={name:"nova/applicationCenter.md"};function s(p,o,l,i,g,d){return a(),t("div",null,o[0]||(o[0]=[e('<div style="display:none;" hidden="true" aria-hidden="true">Are you an LLM? You can read better optimized documentation at /01.指南/10.使用/12.应用发布.md for this page in Markdown format</div><h1 id="应用中心" tabindex="-1">应用中心 <a class="header-anchor" href="#应用中心" aria-label="Permalink to &quot;应用中心&quot;">​</a></h1><p>✨ <strong>特性:</strong></p><ul><li>以应用为中心: 告别资源碎片化管理，一站式管理应用全生命周期</li><li>多版本管理: 支持应用多版本发布、回滚、灰度发布</li><li>服务聚合: 自动关联 Service、Ingress，无需频繁切换页面</li><li>便捷运维: Pod 管理、副本伸缩、镜像更新、配置变更一键完成</li><li>全类型支持: Deployment、StatefulSet、DaemonSet、CronJob 全覆盖</li></ul><h2 id="设计理念" tabindex="-1">设计理念 <a class="header-anchor" href="#设计理念" aria-label="Permalink to &quot;设计理念&quot;">​</a></h2><h3 id="为什么需要以应用为中心" tabindex="-1">为什么需要以应用为中心？ <a class="header-anchor" href="#为什么需要以应用为中心" aria-label="Permalink to &quot;为什么需要以应用为中心？&quot;">​</a></h3><p>在传统的 Kubernetes 管理方式中，运维人员需要分别管理 Deployment、Service、Ingress、ConfigMap 等多种资源。当一个业务应用包含多个组件时，运维人员不得不在不同的资源页面之间频繁切换，这不仅效率低下，而且容易出错。</p><p>以一个典型的微服务应用为例，它可能包含以下资源：一个 Deployment 管理 Pod 副本、一个或多个 Service 暴露服务端口、一个 Ingress 提供外部访问入口、若干 ConfigMap 和 Secret 存储配置。在传统模式下，查看这个应用的状态需要访问至少 4-5 个不同的页面。</p><h3 id="以应用为中心的解决方案" tabindex="-1">以应用为中心的解决方案 <a class="header-anchor" href="#以应用为中心的解决方案" aria-label="Permalink to &quot;以应用为中心的解决方案&quot;">​</a></h3><p>Kube Nova 的应用中心采用「以应用为中心」的设计理念，将与应用相关的所有资源聚合到一个统一的管理视图中。这种设计带来了三个核心价值：</p><p><strong>第一，信息聚合，一目了然。</strong> 在应用详情页面，您可以看到应用的 Pod 状态、Service 端口、Ingress 地址、资源使用等所有关键信息，无需在多个页面之间跳转。</p><p><strong>第二，操作集中，提升效率。</strong> 镜像更新、副本伸缩、配置变更、服务管理等常用操作都集中在应用页面内完成，大幅减少操作步骤，提升运维效率。</p><p><strong>第三，版本管理，轻松回滚。</strong> 每次应用变更都会记录为一个版本，支持快速查看历史版本、对比差异、一键回滚，让应用发布更加安全可控。</p><h3 id="应用类型说明" tabindex="-1">应用类型说明 <a class="header-anchor" href="#应用类型说明" aria-label="Permalink to &quot;应用类型说明&quot;">​</a></h3><p>应用中心支持管理以下类型的 Kubernetes 工作负载：</p><p><strong>Deployment（无状态应用部署）</strong> 是最常用的应用类型，支持滚动更新和水平扩展，适用于 Web 服务、API 网关、微服务等无状态应用。其特点是副本之间完全对等，可以随时扩缩容，支持滚动更新策略。</p><p><strong>StatefulSet（有状态应用部署）</strong> 提供稳定的网络标识和持久化存储，适用于数据库、消息队列、分布式存储等有状态应用。其特点是每个 Pod 有固定的网络标识（如 pod-0、pod-1），支持有序部署和删除，可以绑定持久化存储卷。</p><p><strong>DaemonSet（守护进程集）</strong> 确保在每个节点上运行一个 Pod 副本，适用于日志收集、监控代理、网络插件等系统守护进程。其特点是全节点部署，新节点加入时自动调度，适合需要在每个节点运行的基础服务。</p><p><strong>CronJob（定时任务）</strong> 按照指定的时间表周期性运行任务，适用于定时备份、数据同步、报表生成等周期性任务。其特点是支持 Cron 表达式定义执行周期，任务完成后 Pod 自动清理。</p><div class="note custom-block github-alert"><p class="custom-block-title">为什么不包含 Pod 和 Job？</p><p>Pod 和 Job 是一次性或短生命周期的资源，不具备持续的生命周期管理需求。Pod 通常由上层控制器（Deployment、StatefulSet 等）管理，而 Job 在任务完成后即终止。应用中心聚焦于需要持续运维的长生命周期应用，因此 Pod 和 Job 由专门的管理模块处理。</p></div><hr><h2 id="应用列表" tabindex="-1">应用列表 <a class="header-anchor" href="#应用列表" aria-label="Permalink to &quot;应用列表&quot;">​</a></h2><p><strong>操作路径</strong>：业务中心 → 应用中心</p><h3 id="页面布局" tabindex="-1">页面布局 <a class="header-anchor" href="#页面布局" aria-label="Permalink to &quot;页面布局&quot;">​</a></h3><p>应用中心页面采用左右分栏布局，左侧为服务列表，右侧为应用详情。</p><p><strong>顶部筛选栏</strong> 提供多维度的筛选条件：</p><ul><li><strong>集群</strong>：选择要查看的目标集群</li><li><strong>工作空间</strong>：选择工作空间（对应 Kubernetes Namespace）</li><li><strong>服务</strong>：选择具体的服务/应用</li><li><strong>类型</strong>：按应用类型筛选（全部、Deployment、StatefulSet、DaemonSet、CronJob）</li></ul><h3 id="服务列表" tabindex="-1">服务列表 <a class="header-anchor" href="#服务列表" aria-label="Permalink to &quot;服务列表&quot;">​</a></h3><p>左侧的服务列表展示当前工作空间内的所有应用，包括：</p><p><strong>应用名称</strong> 显示应用的名称，点击可切换查看不同应用的详情。</p><p><strong>应用类型标签</strong> 通过彩色标签标识应用类型：</p><ul><li><code>deployment</code>（蓝色）：无状态应用</li><li><code>statefulset</code>（红色）：有状态应用</li><li><code>daemonset</code>（橙色）：守护进程集</li><li><code>cronjob</code>（紫色）：定时任务</li></ul><p><strong>搜索功能</strong> 支持按名称搜索应用，快速定位目标服务。</p><p><strong>刷新按钮</strong> 点击可刷新服务列表，获取最新的应用状态。</p><hr><h2 id="基础信息" tabindex="-1">基础信息 <a class="header-anchor" href="#基础信息" aria-label="Permalink to &quot;基础信息&quot;">​</a></h2><p>基础信息页面展示应用的概览信息和访问地址，是了解应用状态的入口。</p><h3 id="应用状态概览" tabindex="-1">应用状态概览 <a class="header-anchor" href="#应用状态概览" aria-label="Permalink to &quot;应用状态概览&quot;">​</a></h3><p>页面顶部以卡片形式展示应用的关键指标：</p><p><strong>Pod 总数</strong> 显示应用当前的 Pod 副本总数，反映应用的规模。</p><p><strong>正常 Pod</strong> 显示处于 Running 且就绪的 Pod 数量，以及正常率百分比。100% 表示所有 Pod 都在正常运行。</p><p><strong>异常 Pod</strong> 显示处于非正常状态（Pending、Failed、Unknown 等）的 Pod 数量，以及异常率。0% 是理想状态。</p><p><strong>Service 数量</strong> 显示与该应用关联的 Service 资源数量。</p><p><strong>Ingress 数量</strong> 显示与该应用关联的 Ingress 资源数量。</p><h3 id="服务访问地址" tabindex="-1">服务访问地址 <a class="header-anchor" href="#服务访问地址" aria-label="Permalink to &quot;服务访问地址&quot;">​</a></h3><p>服务访问地址区域聚合展示了应用的所有访问入口，这正是「以应用为中心」理念的直接体现。运维人员无需分别查看 Service 和 Ingress 资源，所有访问信息都集中在这里。</p><p><strong>集群内部地址（ClusterIP）</strong> 列出所有 ClusterIP 类型的 Service 地址，用于集群内部服务间调用。地址格式为 <code>http://{service-name}.{namespace}.svc.cluster.local:{port}</code>。标签上的数字表示 Service 暴露的端口数量。</p><p><strong>节点端口地址（NodePort）</strong> 列出所有 NodePort 类型的 Service 地址，可通过任意节点 IP 加端口访问。</p><p><strong>LoadBalancer 访问地址</strong> 列出所有 LoadBalancer 类型的 Service 地址，适用于云环境，由云厂商提供外部负载均衡器。</p><p><strong>Ingress 访问地址（HTTP/HTTPS）</strong> 列出所有 Ingress 资源配置的域名访问地址，支持 HTTP 和 HTTPS 协议，是应用对外暴露的首选方式。</p><hr><h2 id="版本管理" tabindex="-1">版本管理 <a class="header-anchor" href="#版本管理" aria-label="Permalink to &quot;版本管理&quot;">​</a></h2><p>版本管理是应用中心的核心功能模块，支持应用的多版本管理、Pod 运维、副本伸缩、镜像更新等操作。</p><h3 id="版本概览" tabindex="-1">版本概览 <a class="header-anchor" href="#版本概览" aria-label="Permalink to &quot;版本概览&quot;">​</a></h3><p>版本管理页面顶部展示应用的版本信息：</p><p><strong>发布新版本</strong> 点击「+ 发布新版本」按钮可以创建应用的新版本，用于灰度发布或蓝绿部署场景。</p><p><strong>当前版本卡片</strong> 显示应用的当前稳定版本，绿色边框标识为「稳定版本」。点击版本卡片可展开该版本的详细管理功能。</p><h3 id="pod-管理" tabindex="-1">Pod 管理 <a class="header-anchor" href="#pod-管理" aria-label="Permalink to &quot;Pod 管理&quot;">​</a></h3><p>Pod 管理页签展示当前版本下所有 Pod 的详细信息和运维操作。</p><h4 id="pod-列表" tabindex="-1">Pod 列表 <a class="header-anchor" href="#pod-列表" aria-label="Permalink to &quot;Pod 列表&quot;">​</a></h4><p>Pod 列表以表格形式展示，包含以下信息：</p><p><strong>Pod 名称</strong> 显示 Pod 的完整名称，点击可查看 Pod 详情。</p><p><strong>状态</strong> 显示 Pod 的运行状态，常见状态包括：</p><ul><li><code>Running</code>（绿色）：正常运行中</li><li><code>Pending</code>（黄色）：等待调度或资源</li><li><code>Failed</code>（红色）：运行失败</li><li><code>Terminating</code>（灰色）：正在终止</li></ul><p><strong>就绪</strong> 显示 Pod 内容器的就绪状态，格式为「就绪数/总数」，如 <code>3/3</code> 表示 3 个容器全部就绪。</p><p><strong>重启</strong> 显示 Pod 的累计重启次数，频繁重启可能表示应用存在问题。</p><p><strong>节点</strong> 显示 Pod 所在的节点名称。</p><p><strong>Pod IP</strong> 显示 Pod 的集群内部 IP 地址。</p><p><strong>运行时长</strong> 显示 Pod 从创建到现在的运行时长。</p><p><strong>创建时间</strong> 显示 Pod 的创建时间戳。</p><h4 id="pod-操作" tabindex="-1">Pod 操作 <a class="header-anchor" href="#pod-操作" aria-label="Permalink to &quot;Pod 操作&quot;">​</a></h4><p>每个 Pod 行提供便捷的操作入口：</p><p><strong>日志</strong> 查看 Pod 内容器的实时日志输出，支持多容器切换。</p><p><strong>终端</strong> 打开 Web 终端，直接进入 Pod 内执行命令，用于调试和排查问题。</p><p><strong>文件</strong> 浏览和管理 Pod 内的文件系统，支持文件上传和下载。</p><h4 id="资源操作" tabindex="-1">资源操作 <a class="header-anchor" href="#资源操作" aria-label="Permalink to &quot;资源操作&quot;">​</a></h4><p>点击「资源操作」下拉菜单，可对整个应用进行批量操作：</p><p><strong>查看 YAML</strong> 查看应用的完整 YAML 配置，支持复制和下载。</p><p><strong>查看详情</strong> 查看应用资源的详细信息，包括标签、注解、事件等。</p><p><strong>停止变更</strong> 暂停正在进行的滚动更新操作。</p><p><strong>恢复变更</strong> 恢复被暂停的滚动更新操作。</p><p><strong>重启资源</strong> 触发应用的滚动重启，所有 Pod 会被依次重建。</p><p><strong>停止资源</strong> 将应用的副本数设置为 0，停止所有 Pod。</p><p><strong>启动资源</strong> 恢复应用的副本数，重新启动 Pod。</p><h3 id="副本管理" tabindex="-1">副本管理 <a class="header-anchor" href="#副本管理" aria-label="Permalink to &quot;副本管理&quot;">​</a></h3><p>副本管理页签用于管理应用的副本数量和自动伸缩策略。</p><h4 id="当前状态" tabindex="-1">当前状态 <a class="header-anchor" href="#当前状态" aria-label="Permalink to &quot;当前状态&quot;">​</a></h4><p>显示应用的当前副本状态：</p><p><strong>当前副本数</strong> 显示正在运行的 Pod 副本数量。</p><p><strong>应用类型</strong> 显示工作负载类型（Deployment、StatefulSet 等）。</p><p><strong>HPA 状态</strong> 显示水平自动伸缩（HPA）的配置状态，「未配置」表示未启用自动伸缩。</p><p><strong>VPA 状态</strong> 显示垂直自动伸缩（VPA）的配置状态。</p><h4 id="扩缩容配置" tabindex="-1">扩缩容配置 <a class="header-anchor" href="#扩缩容配置" aria-label="Permalink to &quot;扩缩容配置&quot;">​</a></h4><p><strong>目标副本数</strong> 通过加减按钮或直接输入数字来调整目标副本数。</p><p><strong>变化趋势</strong> 可视化展示从当前副本数到目标副本数的变化方向（增加、减少或无变化）。</p><p><strong>应用更改</strong> 点击「应用更改」按钮执行扩缩容操作。</p><p><strong>重置</strong> 点击「重置」按钮恢复为当前副本数。</p><h4 id="扩缩容说明" tabindex="-1">扩缩容说明 <a class="header-anchor" href="#扩缩容说明" aria-label="Permalink to &quot;扩缩容说明&quot;">​</a></h4><p>页面右侧提供扩缩容操作的说明：</p><p><strong>扩容（增加副本）</strong> 系统会创建新的 Pod，逐步增加到目标数量。新 Pod 会自动加入负载均衡，开始接收流量。</p><p><strong>缩容（减少副本）</strong> 系统会优雅地终止多余的 Pod，直到达到目标数量。Pod 会先从负载均衡中移除，然后执行 PreStop 钩子，最后终止。</p><div class="warning custom-block github-alert"><p class="custom-block-title">重要提示</p><p>缩容会导致部分 Pod 被终止，请确保服务有足够的副本处理当前流量，避免服务中断。</p></div><h3 id="更新管理" tabindex="-1">更新管理 <a class="header-anchor" href="#更新管理" aria-label="Permalink to &quot;更新管理&quot;">​</a></h3><p>更新管理页签用于更新应用的容器镜像和更新策略。</p><h4 id="更新镜像" tabindex="-1">更新镜像 <a class="header-anchor" href="#更新镜像" aria-label="Permalink to &quot;更新镜像&quot;">​</a></h4><p>镜像更新是应用发布的核心操作，支持以下配置：</p><p><strong>容器选择</strong> 当应用包含多个容器时，选择要更新的目标容器。</p><p><strong>当前镜像</strong> 显示容器当前使用的镜像地址和版本标签。</p><p><strong>镜像搜索</strong> 支持搜索平台镜像仓库中的镜像，输入镜像名称（如 nginx、redis）进行搜索。</p><p><strong>镜像配置</strong> 配置新的镜像地址：</p><ul><li><strong>镜像仓库</strong>：选择镜像所在的仓库</li><li><strong>镜像名称</strong>：输入或选择镜像名称</li><li><strong>镜像标签</strong>：选择镜像版本标签</li></ul><h4 id="更新策略" tabindex="-1">更新策略 <a class="header-anchor" href="#更新策略" aria-label="Permalink to &quot;更新策略&quot;">​</a></h4><p>配置应用的滚动更新策略（仅 Deployment 支持）：</p><p><strong>最大不可用（maxUnavailable）</strong> 更新过程中允许不可用的最大 Pod 数量或百分比。</p><p><strong>最大超量（maxSurge）</strong> 更新过程中允许超出期望副本数的最大 Pod 数量或百分比。</p><h4 id="历史版本" tabindex="-1">历史版本 <a class="header-anchor" href="#历史版本" aria-label="Permalink to &quot;历史版本&quot;">​</a></h4><p>查看应用的历史发布版本，支持版本对比和一键回滚：</p><p><strong>版本列表</strong> 显示所有历史版本的修订号、创建时间、变更内容。</p><p><strong>版本回滚</strong> 选择目标版本，一键回滚到该版本的配置。</p><h3 id="高级管理" tabindex="-1">高级管理 <a class="header-anchor" href="#高级管理" aria-label="Permalink to &quot;高级管理&quot;">​</a></h3><p>高级管理页签提供容器级别的详细配置管理。</p><h4 id="环境变量" tabindex="-1">环境变量 <a class="header-anchor" href="#环境变量" aria-label="Permalink to &quot;环境变量&quot;">​</a></h4><p>管理容器的环境变量配置：</p><p><strong>容器选择</strong> 选择要配置的目标容器。</p><p><strong>环境变量列表</strong> 展示当前配置的所有环境变量：</p><ul><li><strong>变量名</strong>：环境变量名称</li><li><strong>来源类型</strong>：值的来源方式（直接值、ConfigMap、Secret、字段引用）</li><li><strong>值/引用</strong>：具体的值或引用配置</li></ul><p><strong>批量导入</strong> 支持批量导入环境变量配置。</p><p><strong>添加变量</strong> 点击「+ 添加变量」按钮新增环境变量。</p><h4 id="健康检查" tabindex="-1">健康检查 <a class="header-anchor" href="#健康检查" aria-label="Permalink to &quot;健康检查&quot;">​</a></h4><p>配置容器的健康检查探针：</p><p><strong>存活探针（Liveness Probe）</strong> 检测容器是否存活，失败时重启容器。</p><p><strong>就绪探针（Readiness Probe）</strong> 检测容器是否就绪，失败时从 Service 端点移除。</p><p><strong>启动探针（Startup Probe）</strong> 检测容器是否启动完成，适用于启动较慢的应用。</p><h4 id="资源配额" tabindex="-1">资源配额 <a class="header-anchor" href="#资源配额" aria-label="Permalink to &quot;资源配额&quot;">​</a></h4><p>配置容器的资源请求（requests）和限制（limits）：</p><p><strong>CPU 请求/限制</strong> 设置 CPU 资源的请求值和上限。</p><p><strong>内存请求/限制</strong> 设置内存资源的请求值和上限。</p><h4 id="调度管理" tabindex="-1">调度管理 <a class="header-anchor" href="#调度管理" aria-label="Permalink to &quot;调度管理&quot;">​</a></h4><p>配置 Pod 的调度策略：</p><p><strong>节点选择器</strong> 通过标签选择 Pod 调度的目标节点。</p><p><strong>亲和性/反亲和性</strong> 配置节点亲和性和 Pod 亲和性/反亲和性规则。</p><p><strong>容忍度</strong> 配置 Pod 对节点污点的容忍规则。</p><h4 id="存储管理" tabindex="-1">存储管理 <a class="header-anchor" href="#存储管理" aria-label="Permalink to &quot;存储管理&quot;">​</a></h4><p>管理容器的存储卷挂载：</p><p><strong>存储卷列表</strong> 展示已挂载的存储卷（PVC、ConfigMap、Secret、EmptyDir 等）。</p><p><strong>添加存储卷</strong> 添加新的存储卷挂载配置。</p><h3 id="事件管理" tabindex="-1">事件管理 <a class="header-anchor" href="#事件管理" aria-label="Permalink to &quot;事件管理&quot;">​</a></h3><p>事件管理页签展示应用相关的 Kubernetes 事件，用于排查问题和了解应用状态变化。</p><hr><h2 id="服务管理" tabindex="-1">服务管理 <a class="header-anchor" href="#服务管理" aria-label="Permalink to &quot;服务管理&quot;">​</a></h2><p>服务管理页签用于管理与应用关联的 Service 资源。</p><h3 id="功能说明" tabindex="-1">功能说明 <a class="header-anchor" href="#功能说明" aria-label="Permalink to &quot;功能说明&quot;">​</a></h3><p><strong>查看服务</strong> 列出与应用关联的所有 Service，包括类型、端口、选择器等信息。</p><p><strong>创建服务</strong> 为应用创建新的 Service，支持 ClusterIP、NodePort、LoadBalancer 类型。</p><p><strong>编辑服务</strong> 修改现有 Service 的配置，如端口映射、服务类型等。</p><p><strong>删除服务</strong> 删除不再需要的 Service 资源。</p><hr><h2 id="ingress-管理" tabindex="-1">Ingress 管理 <a class="header-anchor" href="#ingress-管理" aria-label="Permalink to &quot;Ingress 管理&quot;">​</a></h2><p>Ingress 管理页签用于管理应用的外部访问入口。</p><h3 id="功能说明-1" tabindex="-1">功能说明 <a class="header-anchor" href="#功能说明-1" aria-label="Permalink to &quot;功能说明&quot;">​</a></h3><p><strong>查看 Ingress</strong> 列出与应用关联的所有 Ingress 规则。</p><p><strong>创建 Ingress</strong> 为应用创建新的 Ingress，配置域名、路径、TLS 证书等。</p><p><strong>编辑 Ingress</strong> 修改现有 Ingress 的路由规则。</p><p><strong>删除 Ingress</strong> 删除不再需要的 Ingress 资源。</p><hr><h2 id="flagger-发布" tabindex="-1">Flagger 发布 <a class="header-anchor" href="#flagger-发布" aria-label="Permalink to &quot;Flagger 发布&quot;">​</a></h2><p>Flagger 发布页签支持基于 Flagger 的渐进式交付能力，实现金丝雀发布、A/B 测试等高级发布策略。</p><h3 id="功能说明-2" tabindex="-1">功能说明 <a class="header-anchor" href="#功能说明-2" aria-label="Permalink to &quot;功能说明&quot;">​</a></h3><p><strong>金丝雀发布</strong> 逐步将流量从旧版本切换到新版本，根据指标自动推进或回滚。</p><p><strong>A/B 测试</strong> 基于请求特征（Header、Cookie 等）将流量路由到不同版本。</p><p><strong>蓝绿部署</strong> 维护两个相同的环境，一键切换流量。</p><hr><h2 id="操作审计" tabindex="-1">操作审计 <a class="header-anchor" href="#操作审计" aria-label="Permalink to &quot;操作审计&quot;">​</a></h2><p>操作审计页签记录所有对应用的操作历史，便于追溯和审计。</p><h3 id="审计记录" tabindex="-1">审计记录 <a class="header-anchor" href="#审计记录" aria-label="Permalink to &quot;审计记录&quot;">​</a></h3><p><strong>操作时间</strong> 显示操作发生的时间戳。</p><p><strong>操作人</strong> 显示执行操作的用户。</p><p><strong>操作类型</strong> 显示操作的类型（创建、更新、删除、扩缩容等）。</p><p><strong>操作详情</strong> 显示操作的具体内容和变更详情。</p><hr><h2 id="创建工作负载" tabindex="-1">创建工作负载 <a class="header-anchor" href="#创建工作负载" aria-label="Permalink to &quot;创建工作负载&quot;">​</a></h2><p>应用中心支持创建多种类型的工作负载。</p><p><strong>操作路径</strong>：应用中心 → 点击右上角「+」按钮</p><h3 id="工作负载类型选择" tabindex="-1">工作负载类型选择 <a class="header-anchor" href="#工作负载类型选择" aria-label="Permalink to &quot;工作负载类型选择&quot;">​</a></h3><p>创建工作负载时，首先选择工作负载类型：</p><p><strong>Deployment</strong> 无状态应用部署，支持滚动更新和水平扩展。适用场景包括 Web 服务、API 服务、微服务应用。关键特性是无状态、副本管理、滚动更新。</p><p><strong>StatefulSet</strong> 有状态应用部署，提供稳定的网络标识和持久化存储。适用场景包括数据库、消息队列、分布式存储。关键特性是有状态、稳定标识、持久存储。</p><p><strong>DaemonSet</strong> 在每个节点上运行一个 Pod 副本，适合系统守护进程。适用场景包括日志采集、监控代理、网络插件。关键特性是节点守护、全节点部署、日志/监控。</p><p><strong>CronJob</strong> 按照指定的时间表周期性运行任务。适用场景包括定时备份、数据同步、报表生成。关键特性是定时执行、周期性、Cron 表达式。</p><p><strong>Job</strong> 一次性任务，运行完成后自动退出。适用场景包括数据迁移、批处理任务。关键特性是一次性、批处理、任务完成退出。</p><p><strong>Pod</strong> Kubernetes 最小的可部署单元，包含一个或多个容器。适用场景包括快速测试、临时调试。关键特性是单实例、适合测试、快速部署。</p><p>选择类型后，点击「确认创建」按钮进入详细配置页面。</p><hr><h2 id="不同应用类型的便捷操作" tabindex="-1">不同应用类型的便捷操作 <a class="header-anchor" href="#不同应用类型的便捷操作" aria-label="Permalink to &quot;不同应用类型的便捷操作&quot;">​</a></h2><p>不同类型的应用在版本管理中提供的操作略有不同，以下是各类型的特有操作说明。</p><h3 id="deployment-便捷操作" tabindex="-1">Deployment 便捷操作 <a class="header-anchor" href="#deployment-便捷操作" aria-label="Permalink to &quot;Deployment 便捷操作&quot;">​</a></h3><p>Deployment 是功能最完整的应用类型，支持以下便捷操作：</p><p><strong>滚动更新</strong> 支持配置 maxUnavailable 和 maxSurge 策略，实现零停机更新。</p><p><strong>副本伸缩</strong> 支持手动调整副本数，以及配置 HPA 实现自动伸缩。</p><p><strong>暂停/恢复</strong> 支持暂停正在进行的滚动更新，用于观察新版本状态。</p><p><strong>回滚</strong> 支持一键回滚到任意历史版本。</p><p><strong>重启</strong> 支持滚动重启所有 Pod，不改变配置。</p><h3 id="statefulset-便捷操作" tabindex="-1">StatefulSet 便捷操作 <a class="header-anchor" href="#statefulset-便捷操作" aria-label="Permalink to &quot;StatefulSet 便捷操作&quot;">​</a></h3><p>StatefulSet 在 Deployment 的基础上，针对有状态应用提供额外功能：</p><p><strong>有序部署</strong> Pod 按照 0、1、2... 的顺序依次创建，确保依赖关系。</p><p><strong>有序删除</strong> 缩容时按照逆序删除，先删除编号大的 Pod。</p><p><strong>持久存储</strong> 自动为每个 Pod 创建独立的 PVC，数据持久保存。</p><p><strong>稳定网络</strong> 每个 Pod 有固定的 DNS 名称，重建后保持不变。</p><h3 id="daemonset-便捷操作" tabindex="-1">DaemonSet 便捷操作 <a class="header-anchor" href="#daemonset-便捷操作" aria-label="Permalink to &quot;DaemonSet 便捷操作&quot;">​</a></h3><p>DaemonSet 用于全节点部署场景，提供以下便捷操作：</p><p><strong>全节点部署</strong> 自动在所有节点（或指定节点）上部署 Pod。</p><p><strong>节点感知</strong> 新节点加入集群时自动调度，节点移除时自动清理。</p><p><strong>更新策略</strong> 支持 RollingUpdate 和 OnDelete 两种更新策略。</p><p><strong>节点选择</strong> 支持通过节点选择器指定部署的目标节点。</p><div class="note custom-block github-alert"><p class="custom-block-title">DaemonSet 特殊说明</p><p>DaemonSet 没有副本管理功能，因为其副本数由集群节点数决定。扩缩容通过调整节点选择器实现。</p></div><h3 id="cronjob-便捷操作" tabindex="-1">CronJob 便捷操作 <a class="header-anchor" href="#cronjob-便捷操作" aria-label="Permalink to &quot;CronJob 便捷操作&quot;">​</a></h3><p>CronJob 用于定时任务场景，提供以下便捷操作：</p><p><strong>Cron 表达式</strong> 使用标准 Cron 表达式定义执行周期（如 <code>0 2 * * *</code> 表示每天凌晨 2 点）。</p><p><strong>并发策略</strong> 配置任务并发执行策略（Allow、Forbid、Replace）。</p><p><strong>历史保留</strong> 配置保留的成功/失败任务历史数量。</p><p><strong>手动触发</strong> 支持手动触发一次任务执行，不影响定时计划。</p><p><strong>暂停/恢复</strong> 支持暂停定时任务的调度，不删除已创建的 Job。</p><hr><h2 id="最佳实践" tabindex="-1">最佳实践 <a class="header-anchor" href="#最佳实践" aria-label="Permalink to &quot;最佳实践&quot;">​</a></h2><h3 id="应用管理建议" tabindex="-1">应用管理建议 <a class="header-anchor" href="#应用管理建议" aria-label="Permalink to &quot;应用管理建议&quot;">​</a></h3><p><strong>合理选择应用类型</strong> 根据应用的特性选择合适的工作负载类型。无状态服务使用 Deployment，数据库使用 StatefulSet，系统组件使用 DaemonSet，定时任务使用 CronJob。</p><p><strong>配置健康检查</strong> 为应用配置适当的存活探针和就绪探针，确保 Kubernetes 能够正确判断应用状态，实现自动故障恢复和优雅滚动更新。</p><p><strong>设置资源配额</strong> 为容器设置合理的 CPU 和内存请求/限制，避免资源争抢和 OOM 问题。</p><p><strong>使用滚动更新</strong> 利用 Deployment 的滚动更新能力，配合健康检查，实现零停机发布。</p><h3 id="版本管理建议" tabindex="-1">版本管理建议 <a class="header-anchor" href="#版本管理建议" aria-label="Permalink to &quot;版本管理建议&quot;">​</a></h3><p><strong>语义化版本</strong> 使用语义化版本号（如 v1.2.3）标记镜像，便于追踪和回滚。</p><p><strong>保留历史版本</strong> 合理配置 revisionHistoryLimit，保留足够的历史版本以支持回滚。</p><p><strong>发布前测试</strong> 重大变更前在测试环境验证，确认无误后再发布到生产环境。</p><h3 id="服务暴露建议" tabindex="-1">服务暴露建议 <a class="header-anchor" href="#服务暴露建议" aria-label="Permalink to &quot;服务暴露建议&quot;">​</a></h3><p><strong>内部服务使用 ClusterIP</strong> 仅供集群内部调用的服务使用 ClusterIP 类型。</p><p><strong>外部访问使用 Ingress</strong> 需要对外暴露的服务优先使用 Ingress，便于统一管理域名和证书。</p><p><strong>谨慎使用 NodePort</strong> NodePort 会占用节点端口资源，仅在特殊场景使用。</p><hr><h2 id="常见问题" tabindex="-1">常见问题 <a class="header-anchor" href="#常见问题" aria-label="Permalink to &quot;常见问题&quot;">​</a></h2><h3 id="pod-一直处于-pending-状态怎么办" tabindex="-1">Pod 一直处于 Pending 状态怎么办？ <a class="header-anchor" href="#pod-一直处于-pending-状态怎么办" aria-label="Permalink to &quot;Pod 一直处于 Pending 状态怎么办？&quot;">​</a></h3><p><strong>可能原因 1</strong>：资源不足。检查工作空间的配额是否充足，以及集群是否有可用资源。</p><p><strong>可能原因 2</strong>：调度约束无法满足。检查节点选择器、亲和性规则、容忍度配置是否正确。</p><p><strong>可能原因 3</strong>：PVC 无法绑定。对于 StatefulSet，检查 PVC 是否能够成功创建和绑定。</p><h3 id="滚动更新卡住了怎么处理" tabindex="-1">滚动更新卡住了怎么处理？ <a class="header-anchor" href="#滚动更新卡住了怎么处理" aria-label="Permalink to &quot;滚动更新卡住了怎么处理？&quot;">​</a></h3><p><strong>检查新 Pod 状态</strong> 查看新创建的 Pod 是否正常启动，检查 Pod 事件和日志。</p><p><strong>检查健康检查</strong> 确认就绪探针配置正确，新 Pod 能够通过健康检查。</p><p><strong>暂停更新</strong> 如果需要排查问题，可以先暂停更新，防止继续创建问题 Pod。</p><p><strong>回滚</strong> 如果问题无法快速解决，建议先回滚到上一个稳定版本。</p><h3 id="如何实现零停机更新" tabindex="-1">如何实现零停机更新？ <a class="header-anchor" href="#如何实现零停机更新" aria-label="Permalink to &quot;如何实现零停机更新？&quot;">​</a></h3><p>实现零停机更新需要满足以下条件：</p><p><strong>配置就绪探针</strong> 确保新 Pod 在完全就绪后才接收流量。</p><p><strong>合理配置更新策略</strong> maxUnavailable 不要设置过大，确保始终有足够的 Pod 处理流量。</p><p><strong>优雅终止</strong> 为容器配置适当的 terminationGracePeriodSeconds，确保旧 Pod 能够处理完现有请求。</p><p><strong>PreStop 钩子</strong> 如果需要，配置 PreStop 钩子执行清理操作。</p>',254)]))}const u=r(n,[["render",s]]);export{c as __pageData,u as default};
